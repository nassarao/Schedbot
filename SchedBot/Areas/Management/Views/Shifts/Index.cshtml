@model IEnumerable<SchedbotDTOs.Shift>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ShiftType.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Date)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ShiftTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ShiftType.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShiftTime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ShiftID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ShiftID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ShiftID })
        </td>
    </tr>
}

</table>
